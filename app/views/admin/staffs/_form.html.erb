<%= simple_form_for [:admin, @staff] do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :job %>
  <%= f.input :date_start_work %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>