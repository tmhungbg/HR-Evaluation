<%= simple_form_for [:admin, @period] do |f| %>
  <%= f.input :start_time %>
  <%= f.input :end_time %>
  <%= f.association :staffs, as: :check_boxes, collection: Staff.active %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>